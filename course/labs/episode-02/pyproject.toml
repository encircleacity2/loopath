[project]
name = "loopath"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2",
  "rich>=13"
]

[project.optional-dependencies]
dev = [
  "pytest>=8"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
